/** Extra support — never subscription-gated (safety route). */
import { ExtraSupportScreen } from '@/components/support/ExtraSupportScreen';

export default function EmergencyScreen() {
  return <ExtraSupportScreen />;
}
