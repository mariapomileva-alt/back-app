(function () {
  /* Reserved for future store-driven pricing when subscriptionsPubliclyAvailable is true. */
  var config = window.BACK_PLUS_SITE || { subscriptionsPubliclyAvailable: false };
  if (!config.subscriptionsPubliclyAvailable) {
    return;
  }
})();
