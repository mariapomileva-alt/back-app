/* Paste verified App Store / TestFlight / Play URLs when they exist.
   Optional: set android to a hosted .apk path when a build is available. */
window.BACK_RELEASES = {
  version: "1.0.0",
  ios: "",
  android: "",
  androidStore: "",
};
