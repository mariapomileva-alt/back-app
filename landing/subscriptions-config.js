/**
 * Static-site mirror of config/backPlus.ts → subscriptionsPubliclyAvailable.
 * Keep in sync when toggling website State B (live store links).
 * Planned EUR reference (State A only): monthly ~2.99, annual ~19.99 — see config/backPlus.ts.
 */
window.BACK_PLUS_SITE = {
  subscriptionsPubliclyAvailable: false,
};
