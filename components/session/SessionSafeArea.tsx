import { ScreenContainer } from '@/components/layout/ScreenContainer';

/** Named session wrapper — same safe-area + padding contract on every active tool. */
export const SessionSafeArea = ScreenContainer;
