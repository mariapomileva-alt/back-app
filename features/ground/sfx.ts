/**
 * PLACEHOLDER SFX — dev-generated tone, not production studio audio.
 */
export const groundSfx = {
  step: require('../../audio/sfx/ground-step-placeholder.wav'),
} as const;

/** Very soft, muffled step cue (quieter than distract defaults). */
export const GROUND_STEP_SFX_VOLUME = 0.14;
