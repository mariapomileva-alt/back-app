# Listen runtime artwork

Trimmed copies of `assets/listen/premium-loops/` with ~4% alpha safe padding. Masters are never modified.

Regenerate:

```bash
python3 scripts/trim-listen-artwork.py
```

Bounds and aspect ratios: `manifest.json`.
